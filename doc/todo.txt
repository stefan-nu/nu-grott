1. Verhindern dass zu viele Daten zu PVOutput gesendet werden
PVOut: Update refused for {'KNN0D7403B'} due to time limitation

2. Korrekte Daten an PVOutput senden

3. Kurze Packete verarbeiten
Data less then minimum record length, data not processed

4. Grott data ack record or data record not defined, no processing done

5. Unit Tests erstellen

6. Test Coverage berechnen

7. Prevent Logging messages during test execution

In validate_record it is not clear why first is checked for protocol 05/06
and then later is checked for protocol 02, correct comment as it does not
say anything about 02 and the other protocols, even better define the 
protocols and add a reference on the description

Move CRC to its own module or better use a crc library

Is the code running in polling mode? if so it might be better
to use interrupt mode

Send commands to inverter, e.g. to set time automatically

Dokumentation der Funktionsprototypen fehlt

Some parameters are missing from grott.ini documentation -> add them

Zeitstring an pvoutput war 12:40am das muss 00:40am sein, grott.ini time = server
dateoffset = 136 was bedeutet das?
Inverter Zeit ist oft um stunden falsch